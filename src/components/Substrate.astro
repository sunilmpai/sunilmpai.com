---
---

<div id="substrate-container" style="
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  z-index: -1;
  pointer-events: none;
  opacity: 0.3; /* Adjust opacity to keep your content readable */
"></div>

<script type="module" client:load>
  import { Runtime, Inspector } from 'https://cdn.jsdelivr.net/npm/@observablehq/runtime@5/dist/runtime.js';
  import define from "https://api.observablehq.com/@mbostock/substrate.js?v=3";

  const runtime = new Runtime();

  runtime.module(define, name => {
    if (name === "viewof context") {
      return new Inspector(document.querySelector("#substrate-container"));
    }
    return true; // Skip other notebook cells
  });
</script>
